import Styles from '../../../styles';

const { color, font, padding } = Styles;

export default {
  buttonStyles: {
    fontSize: font.small,
    height: '38px',
    marginButtom: '5px',
    padding: 0,
    width: '180px',
  },
  radioElementStyles: {
    paddingBottom: padding.base,
  },
  roleTitleStyles: {
    color: color.brand,
    fontSize: font.mini,
    marginBottom: 0,
  },
};
